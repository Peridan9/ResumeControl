/**
 * Timing constants
 * Centralized timing values for debouncing, delays, and message durations
 */

/**
 * Debounce delay for form auto-save (in milliseconds)
 * Used in ApplicationForm for saving draft to sessionStorage
 */
export const DEBOUNCE_DELAY = 300

/**
 * Delay before closing modals after success (in milliseconds)
 * Allows users to see success messages before modal closes
 */
export const MODAL_CLOSE_DELAY = 500

/**
 * Duration to show success messages (in milliseconds)
 * Used for toast notifications and inline success messages
 */
export const SUCCESS_MESSAGE_DURATION = 3000

/**
 * Duration to show error messages (in milliseconds)
 * Used for toast notifications and inline error messages
 */
export const ERROR_MESSAGE_DURATION = 5000



